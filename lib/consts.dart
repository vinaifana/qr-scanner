import 'package:flutter/material.dart';

// A Primary Color Definition
const primaryColor = Color(0xFF404CCF);

// A Secondary Color Definition
const secondaryColor = Color(0xFF22D566);
const textColor = Color(0xFF757575);
const textBlue = Color(0xFF0D135A);
const blackDefault = Color(0xFF323232);

// A Tertiary Color Definition
const tertiaryColor = Color(0xFFF2C94C);

// animationDuration time definition
const animationDuration = Duration(milliseconds: 200);

// default const value for padding size
const defaultPadding = 20.0;